# Object-Segmentation-Model
A machine learning pipeline that segments individual objects on a tabletop using RGB and depth camera data. The system uses techniques like plane detection and clustering to identify and separate overlapping objects, generating instance masks for downstream analysis.
